{
  "name": "alpha",
  "displayName": "Alpha",
  "description": "OMP-style local harness as a VS Code chat participant using VS Code/Copilot model access.",
  "version": "0.0.1",
  "publisher": "local",
  "private": true,
  "engines": {
    "vscode": "^1.95.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onChatParticipant:alpha.participant",
    "onCommand:alpha.openPendingEdits",
    "onCommand:alpha.applyPendingEdit",
    "onCommand:alpha.clearPendingEdits"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "chatParticipants": [
      {
        "id": "alpha.participant",
        "name": "alpha",
        "fullName": "Alpha",
        "description": "OMP-style local coding harness with read/bash/edit/search/find/write/todo tools."
      }
    ],
    "commands": [
      {
        "command": "alpha.openPendingEdits",
        "title": "Alpha: Open Pending Edits"
      },
      {
        "command": "alpha.applyPendingEdit",
        "title": "Alpha: Apply Pending Edit"
      },
      {
        "command": "alpha.clearPendingEdits",
        "title": "Alpha: Clear Pending Edits"
      }
    ],
    "configuration": {
      "title": "Alpha",
      "properties": {
        "alpha.search.maxResults": {
          "type": "number",
          "default": 80,
          "minimum": 1,
          "maximum": 500,
          "description": "Maximum search matches returned by the local search tool."
        },
        "alpha.read.maxBytes": {
          "type": "number",
          "default": 200000,
          "minimum": 1000,
          "description": "Maximum bytes read from a single file."
        },
        "alpha.read.defaultLimit": {
          "type": "number",
          "default": 200,
          "minimum": 1,
          "maximum": 2000,
          "description": "Maximum lines returned for a file read without an explicit selector."
        },
        "alpha.tools.discoveryMode": {
          "type": "string",
          "enum": [
            "off",
            "all"
          ],
          "default": "off",
          "description": "Tool discovery mode. off advertises all enabled Alpha public tools. all advertises only essential tools; search_tool_bm25 support will activate discoverable tools in a later phase."
        }
      }
    }
  },
  "scripts": {
    "compile": "tsc -p .",
    "test": "npm run compile && node --test test/*.test.mjs",
    "watch": "tsc -watch -p ."
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "@types/vscode": "^1.95.0",
    "typescript": "^5.6.0"
  }
}
